import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Price Globe — Internationaler Preisvergleich",
  description: "Vergleiche Preise für Hotels, Gastro, Lebensmittel und Transport zwischen USA, Deutschland und Thailand. Finde die günstigsten Reisemonate.",
  openGraph: {
    title: "Price Globe — Internationaler Preisvergleich",
    description: "Live Preisentwicklung: USA vs. Deutschland vs. Thailand",
    type: "website",
  },
};

export const viewport: Viewport = {
  themeColor: "#09090b",
  width: "device-width",
  initialScale: 1,
  maximumScale: 5,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="de"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
